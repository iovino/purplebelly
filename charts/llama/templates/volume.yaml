apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llama-models-pvc
  namespace: llama
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: llama-models
  resources:
    requests:
      storage: 10Gi
