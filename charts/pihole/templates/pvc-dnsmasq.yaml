apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dnsmasq-config-pvc
  namespace: pihole
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
